# Optional: Anthropic API key that powers the AI Chat Explorer and the optional LLM summaries.
# The Classic Explorer works without it. Get a key from: https://console.anthropic.com/
# (API Keys section; pay-as-you-go, billed per token). Then set it below and restart the app.
ANTHROPIC_API_KEY=""
